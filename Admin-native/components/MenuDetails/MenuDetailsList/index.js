import React, {useRef, useState} from 'react';import {    View,    Text,    ScrollView,    ImageBackground} from 'react-native';import ItemCard from './ItemCard';import {styles} from './style';import {Modalize} from "react-native-modalize";export default ({data}) => {    const modalRef = useRef(null);    const [dataInfo, setDataInfo] = useState({});    return (        <ScrollView style={styles.detailList}>            {data.map(item => (                <ItemCard key={item.id} modalRef={modalRef} setDataInfo={setDataInfo} item={item}/>            ))}            <View>                <Modalize modalStyle={styles.modal} handleStyle={styles.modalHandle} withReactModal={true} ref={modalRef}>                    <ImageBackground source={require('../../../img/bgc.jpg')} style={styles.projectBgc}>                        <Text>{dataInfo.title}</Text>                    </ImageBackground>                </Modalize>            </View>        </ScrollView>    );};