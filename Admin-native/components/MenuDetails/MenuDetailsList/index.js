import React from 'react';import {    View,    Text,    FlatList,    TouchableOpacity,    Image,} from 'react-native';import {styles} from './style';export default ({data}) => {    const renderDetailListCell = (item, index) => {        return (            <TouchableOpacity activeOpacity={1}>                <View style={styles.detailListCellContainer}>                    <View style={styles.detailListCellLastIndexContentViewContainer}>                        <Image                            style={styles.imgDish}                            source={{uri: item.img}}                        />                        <View style={styles.detailListCellContentViewBottomContainer}>                            <Text style={styles.title}>{item.title}</Text>                            <Text style={styles.price}>$ {item.price}</Text>                        </View>                    </View>                </View>            </TouchableOpacity>        );    };    return (        <View style={styles.detailList}>            <FlatList                showsVerticalScrollIndicator={false}                data={data}                renderItem={({item, index}) => renderDetailListCell(item, index)}            />        </View>    );};