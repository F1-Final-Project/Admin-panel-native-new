import React from "react";import {    View,    Text,    Image,    TouchableOpacity} from 'react-native';import {styles} from "../style";export default ({item, modalRef, setDataInfo}) => {    const onOpen = () => {        const modal = modalRef.current;        if (modal) modal.open();        setDataInfo(item);    };    return (        <View>            <View style={styles.detailListCellContainer}>                <View style={styles.detailListCellLastIndexContentViewContainer}>                    <Image                        style={styles.imgDish}                        source={{uri: item.img}}                    />                    <View style={styles.detailListCellContentViewBottomContainer}>                        <TouchableOpacity onPress={onOpen} id={item.id}>                            <Text style={styles.title}>{item.title}</Text>                            <Text style={styles.price}>$ {item.price}</Text>                        </TouchableOpacity>                    </View>                </View>            </View>        </View>    )}