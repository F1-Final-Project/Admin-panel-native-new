import React, {useEffect, useState} from 'react'import {    ImageBackground,    StyleSheet,    ScrollView,    Dimensions,    View,    SafeAreaView,    Text} from "react-native";import Header from './Header';import Search from './Search';import NavBar from './NavBar';import MenuList from './MenuList';import {styles} from './style';import Filter from '../../services/Filters';import withHoc from './indexHoc';export default withHoc((props) => {    const {categoryAll} = props.data;    return (        <ImageBackground source={require('../../img/bgc.jpg')} style={styles.projectBgc}>            <SafeAreaView style={styles.mainContainer}>                <Header/>                <ScrollView style={styles.mainContainer}>                    <View>                        <Text style={styles.mainPhrase}>                            What you want to eat today?                        </Text>                        <Search/>                        <NavBar data={categoryAll ? categoryAll : []}/>                        <Text style={styles.secondPhrase}>                            Category Menu                        </Text>                        <MenuList data={Filter.filterCategoryMenu(categoryAll)}/>                        <Text style={styles.secondPhrase}>                            Drink Menu                        </Text>                        <MenuList data={Filter.filterDrink(categoryAll)}/>                    </View>                </ScrollView>            </SafeAreaView>        </ImageBackground>    );});